<h1><%= @task.title %></h1>

<h3><%= @task.details %></h3>

<p>
  <% if @task.completed %>
    Completed
  <% else %>
    Not completed
  <% end %>
</p>

<%= link_to 'Edit', edit_path %> |
<%= link_to 'Delete', task_path, method: :delete %>
<p><%= link_to 'Back to list', tasks_path %></p>
